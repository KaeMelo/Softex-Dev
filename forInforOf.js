const guitar = { color: "Green", brand: "Fender", model: "Stratocaster",
}
const car = ["Azera", "Hyundai", "2021"]

for (const i in guitar) {
  console.log(guitar[i]);
}

for (const x of car) {
  console.log(x);
}
